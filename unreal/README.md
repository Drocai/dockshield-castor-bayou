# Unreal Project Placeholder

Create the Unreal project only after Unreal Engine 5.7.x is installed.

Recommended project name:

```text
DockShield
```

Recommended location:

```text
<repo-root>/DockShield/
```

Do not hand-create `.uproject`, `.uasset`, `.umap`, or generated Unreal files.

